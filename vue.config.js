module.exports={
	// publicPath:"ele/dist/",//打包文件相对路径
    devServer:{
        // proxy: {
		// 	// 配置跨域
		// 	'/api': {
		// 		target: 'https://ele-interface.herokuapp.com/api/',
		// 		ws: true,
		// 		changOrigin: true,//开启代理，在本地创建一个虚拟服务端，然后发送请求的数据，并同时接收请求的数据
		// 		pathRewrite: {
		// 			'^/api': ''
		// 		}
		// 	}
		// }
    
    }
}